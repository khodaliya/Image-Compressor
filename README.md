# Image Compressor code in Python

1. Create a new Folder is "Images" for insert images <br />
2. Run the "image compressor" file <br />
3. makking a new Folder is "Image Compressor" for collaction is output images
